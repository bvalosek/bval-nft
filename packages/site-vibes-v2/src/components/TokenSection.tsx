import React, { FunctionComponent } from 'react';

interface Props {
  limit?: number;
  offset?: number;
  owner?: string;
  creator?: string;
  seeder?: string;
}

export const TokenSection: FunctionComponent<Props> = () => {
  return <></>;
};
